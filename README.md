# gpukernel
Real-time GPU compute in the browser via WebGPU compute shaders (WGSL). Runs a parallel N-body particle simulation on the GPU and benchmarks GPU vs CPU throughput on the same kernel, with an automatic CPU fallback when WebGPU is unavailable. TypeScript + Next.js, deployed on Vercel.
